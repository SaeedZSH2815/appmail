import 'package:flutter/material.dart';

class FilterSreen extends StatelessWidget {
  const FilterSreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Filter"),
    );
  }
}
